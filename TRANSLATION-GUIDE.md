# 翻译规范（中 → 英）

本文件是 `content/**/*.en.md` 的写作与验收依据。写这个站的英文版时，
先读本文件，再读要译的那一篇。

---

## 〇、先看这里：哪些语言要手写

| 语言 | 代码 | 谁来做 |
| --- | --- | --- |
| 简体中文 | `zh-hans` | **手写原文**，是唯一的源 |
| English | `en` | **手写翻译**，本规范的全部要求都针对它 |
| 繁體中文 | `zh-hant` | **不用手写**：由 `tools/docsgen.py` 从简体脚本转换（`tools/hant.py`） |

所以本文件说的「翻译」只针对英文。繁体如果发现用字不对，改
`tools/hant.py` 的转换规则，**不要直接改 `docs/zh-hant/` 下的任何文件**
——那两个都是生成物，下次 `make gen` 会被覆盖。

### 网址不用你操心

* **不要手写站点网址。** 语言前缀由模板拼，你写的相对链接（`../tables/mesh.md`）
  在各语言下都会被拼对；写死成 `/editor/keys/` 反而会在英文站的页面上
  把人送回简体那一支。

## 一、这是什么

做 RWR（小兵步枪）地图时要看的一份手册：面板、模型清单、按键、配置文件。

读者是**正在用地图编辑器干活的人**：他们卡在某一步，边做边查。所以要的是
「这个按钮按下去会怎样」「这个模型能不能用」「这堵墙能不能被炸掉」这类
**能直接下手**的答案，不是介绍与推荐。

**这是工作记录，不是宣传册。** 写它的人就是做地图的人，语气本来就平实、简短、
带口语（「千万别把材质错误全部归类成是模板类 Mesh」）。英文版**跟着这个语气走**：
不要顺手升格成官方文档腔，也不要补上原文没写的因果。

## 二、语气

| 要 | 不要 |
| --- | --- |
| 克制、平实、陈述 | 煽情、感叹、营销腔 |
| 短句，一句一件事 | 长串从句堆叠 |
| 具体到日期、页数、文件名 | 模糊的「曾经」「许多」 |
| 承认材料缺口：「留存材料未见记载」 | 补写合理但无据的细节 |
| 并列保留互相矛盾的说法 | 替读者裁定哪个版本正确 |
| 功能面用公告体（no analytics / is stored / the page does not exist） | 功能面用第二人称（you'll see、we don't…） |
| 标题句首大写：`The nature of the event` | 标题混用 Title Case：`Why This System Is Needed` |
| 引号一律弯引号 `“ ”` | 直引号 `" "` |

中文原文本身已经很有分寸。**英文不要比中文更热情，也不要更冷淡。**

功能面（界面文字、页脚、导航与卡片标签、`description` 等元数据、表格表头、
提示框标题）中文要求公告体，英文同理：**陈述句、不做营销排比、不称读者 you**。
文学面（落地页大字、故事页抒情句、引文）照旧保留原本的语气，不要顺手公文化。

**小标题不要顺手改成更正式的说法**：某一节的小标题是作者写惯了的说法，
译文按对应说法直译即可。改成书面语之后，它与正文里引用的那个名字就对不上了。

## 三、结构与格式（硬性要求）

1. **逐段对应。** 原文有几节、几张表、几个提示框，译文就有几节、几张表、
   几个提示框。不合并、不拆分、不增删。
2. **保留 Markdown 结构**：`##` / `###` 层级、表格行列、`!!! note/warning/info`
   提示框、`<div class="grid cards">` 卡片、`<p class="memorial-kicker">` 小标、
   行内 `<span style="color:#910000">■</span>` 色块。
3. **链接目标原样保留**（它们是镜像的相对路径）：`prepare/index.md`、
   `../tables/wall.md`、`../assets/editor/001.png` 等，
   一个字符都不要改。链接**文字**才翻译。
4. **专名与文件名保持原样**：反引号里的内容（`报告_2026年版.pdf` 之类）是
   磁盘上真实存在的文件名，**一个字符都不要动**；必要时在括号里补一句说明。
   繁体树的转换规则同样保护反引号，见 `tools/hant.py`。
5. **表格表头要译**；`---` 分隔行、数字、日期、色值（`#910000`）不要动。
6. 原文用「方括号引号」`「」` 时，英文用弯引号 `“ ”`。
7. 繁体原文校注（如「孔耀聰」「脈衝星」）按原文保留在引号或反引号内，
   并说明该处为繁体原文。

## 四、术语表

以下译法必须一致。表格里没有的，按上下文判断，但同一篇内保持一致。

译名不统一是多语言站点最常见、最难事后修的毛病，而一张表是唯一能在事前拦住它的。

| 中文 | English |
| --- | --- |
| 地编 / 地图编辑器 | the (map) editor |
| 模板 | template |
| 模型 | model |
| 物件 / 摆件 | object |
| 载具 | vehicle |
| 贴花 | decal |
| 碰撞箱 | collision box |
| 法线 | normal |
| 斜向下视角 / 正对法线 | oblique view / facing the normal |
| 平台 | platform |
| 散布 | scatter |
| 视野（FOV） | field of view |
| 摧毁阈值 | destruction threshold |
| 城堡墙 | castle wall |
| 带刺铁丝网 | barbed wire |
| 参考图 | reference image |
| 待试 | untested |
| 加载时崩溃 | crashes on load |
| 历史版 | archived version |
| 快照 | snapshot |
| 默认语言 | default language |
| 译文 | translation |
| 派生语言 | derived language |
| 脚本转换 | script conversion |
| 指纹 | fingerprint |
| 过期 | stale |
| 漏翻 | missing translation |
| 构建层 / 手写层 | build layer / hand-written layer |
| 共享资产 | shared asset |
| 前置元数据 | front matter |
| 导航 | navigation |
| 面包屑 | breadcrumbs |
| 跳转桩 | redirect stub |

**`template = …` 里的引用名一律不译**：那是磁盘上的真实标识符。清单表「名称」列写成
`小石头` 换行 `template = rock_s1` 的，英文里保留 `template = rock_s1` 原样，
只译前面那半句。

**专名（站名、项目名、人名）单独列一张。** 它们通常整个项目里只有一种写法，
写进表里比每次现译可靠：
如果你的站名在中英两种语言里不同形（很多站名如此），那它是**配置**而不是译文，
应当进 `zensical.toml` 的 `[project.extra]`，见下面第五节。

## 四之二、原始结论政策（重要）

本站的原始资料是**编辑组的内部工作记录**，不是一份已经收敛的说明书。
有三处**必须原样带过去**：

1. **原文的「待试」「未录入」标记。** `settings/map-settings` 那一页原文标着
   「待试」，清单表里成片的备注空着或写着「相关数据（待录入）」。
   译成 `untested` / `to be recorded` 即可，**不要译成肯定句，也不要顺手补上
   你以为的答案**。

2. **原文干脆的否定句。** 「加载时崩溃」「外侧迷彩布无碰撞箱」「无碰撞箱」
   「和上面的一样，这个贴图引用有bug」——这些是**结论**，直译：
   `crashes on load`、`the outer camouflage cloth has no collision box`、
   `no collision box`、`same as above — this texture reference is buggy`。
   不要柔化成 `may cause issues` 之类。

3. **原文的推测。** 清单表里写「下面标未生成的也可能是这个特性」这类，
   英文照写 `the ones marked as not spawning below may be the same quirk`，
   **不要**升级成断言，也不要把推测删掉。

!!! warning "反面示例"
    把「加载时崩溃」译成 `not recommended for use`，或把「待试」整段删掉、只留那张表
    ——那不是翻译，是替编辑组改了它的结论。组里的人靠这些标记决定「这个物件能不能用」，
    改软了就等于没有。
    物件名与 `template` 引用名同理：没有对应英文名的，保留中文音译或直接用引用名，
    **不要替它编一个英文名**——组里对不上号。

## 五、前置元数据（硬性要求）

译文文件的前置元数据必须包含：

```yaml
---
title: <英文标题>
description: <英文描述，一句话>
source_sha256: <原文的 sha256，见下方获取方式>
translated: <今天的日期 YYYY-MM-DD>
---
```

* `nav_label`、`nav`、`icon` 等键**原样保留**（它们是导航用的）。
* `source_sha256` 必须是**你翻译时所依据的那一版原文**的指纹。
  取值方式（在仓库根目录执行）：

  ```bash
  shasum -a 256 content/<路径>.zh-hans.md
  ```

* 若译文必须大量引用中文原文（例如整段引文），在元数据里加 `allow_cjk: true`，
  否则检查器会因中文字符占比过高而报「疑似未翻译」。默认不要加。

## 六、验收

译完之后在仓库根目录跑：

```bash
make gen        # 生成 docs/ 与导航
uv run python tools/i18n_check.py
make build      # 全套：生成 → 构建 → 标签过滤 → 链接体检 → 翻译度体检
```

检查器会逐页比对：

* **结构**：标题层级、表格行数、提示框数、卡片数、代码块数、图片数、链接目标
  ——必须与原文一致；
* **指纹**：`source_sha256` 必须等于当前原文的 sha256，否则报「已过期」；
* **语言**：正文中文字符占比超过 8% 会报「疑似未翻译」。

目标是把该页从「待翻译」变成「已同步」。**报告里任何一条关于你负责页面的
告警都要修掉**，不要留给下一个人。

## 七、常见错误

1. 把 `../assets/...` 链接改成了 `assets/...` —— 不要动链接目标。
   本站的图在 `docs/assets/editor/` 与 `docs/assets/tables/<slug>/` 下，
   译文比简体深一层，多出来的 `../` 由 `docsgen.py` 补，**不要手写**。
2. **改锚点里的中文。** 链接形如 `](settings.md#save说明)`，`#` 之后是标题的 id。
   繁体树上那一份由 `tools/hant.py` 自动转写，**简体与英文里照抄原文即可**；
   手改成 `#save` 或 `#Save说明` 都会落空。
3. 少译一行表格 —— 检查器会报「表格行数对不上」。清单表动辄上百行，
   漏一行它拦得住，**漏一列拦不住**——表头三列（预览 / 名称 / 备注）一列都不能少。
4. 把两个短段落合并成一段 —— 结构对不上。
5. 忘了 `source_sha256` —— 报「未记录指纹」。
6. 把原文的否定句译软 —— 见第四之二节。该加 `allow_cjk` 就加。
7. 擅自补写原文没有的日期、版本号、阈值 —— 这是这座站点最忌讳的事。
